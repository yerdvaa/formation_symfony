{% extends "layout.html.twig" %}

{% block ariane %}
    <li class="active">Modification de produits</li>
{% endblock %}

{% block content %}


        <p>Nom du produit : <strong>{{ product.title }}</strong></p>
        <p>Description : {{ product.description }}</p>
        <p>Prix : {{ product.price }} €</p>
        <!--<p>Date de création : {#{{ produit.date_created | date("d/m/y") }}#} </p>-->
        <p>Quantité: {{ product.quantity }}</p>



        <a href="{{ path("edit_product", {id:product.id}) }}"><button type="button" class="btn bg-purple btn-xs">Modifier</button></a>
    {% endblock %}