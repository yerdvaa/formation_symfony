{% extends "layout.html.twig" %}
{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block ariane %}
    <li class="active">Catégorie</li>
{% endblock %}


{%  block content %}
    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brand.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ brand.title }}</td>
            </tr>
        </tbody>
    </table>


        <a href="{{ path("brand_index") }}"><button type="button" class="btn btn-info">Retour à la liste des catégories</button></a>
            <a href="{{ path('brand_edit', { 'id': brand.id }) }}"><button type="button" class="btn btn-info">Modifier</button></a>

            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}

{% endblock %}
